import { MotionGrid } from "@components/motion";
import { ImageEntry } from "@components/image";

interface ImageDetailsProps {
    src: string;
    image: ImageEntry;
}

export const ImageDetails = () => {
    return <MotionGrid></MotionGrid>
};
